// Arquivo não utilizado mais - substituído por DashboardCompleto.tsx
// Mantido apenas para compatibilidade
export default function DashboardPageSimples() {
  return (
    <div className="flex items-center justify-center h-64">
      <p className="text-gray-500">Este componente foi substituído pelo DashboardCompleto</p>
    </div>
  );
}